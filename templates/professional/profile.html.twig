{% extends 'professional/base_professional.html.twig' %}

{% block title %}My Profile - {{ app.user.companyName }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block body %}
    <div class="drl-card">
        <div class="drl-card-head">
            <div>
                <h4 class="drl-card-title">My Profile</h4>
                <p class="drl-card-sub">View and manage your professional information</p>
            </div>
            <button type="button" class="drl-btn-orange" style="padding: 8px 16px; font-size: 13px;" data-bs-toggle="modal" data-bs-target="#editProfileModal">
                <i class="feather-edit"></i> Edit Profile
            </button>
        </div>

        <div class="p-4">
            <!-- Banner and Profile Photo -->
            <div class="mb-5">
                <div class="tutor-bg-photo position-relative" style="height: 250px; background-image: url('{{ app.user.banner ? asset('uploads/banners/' ~ app.user.banner) : asset('assets/drilleedo/images/bg/bg-image-22.jpg') }}'); background-size: cover; background-position: center; border-radius: 12px;">
                    {# Banner edit button #}
                    <button class="pro-btn-icon" data-bs-toggle="modal" data-bs-target="#editBannerModal"
                            style="position: absolute; top: 14px; right: 14px; background: rgba(0,0,0,0.45); border: 2px solid rgba(255,255,255,0.5); backdrop-filter: blur(4px); width: 38px; height: 38px; border-radius: 50%; color: white; display: flex; align-items: center; justify-content: center; cursor: pointer; transition: all 0.2s;"
                            title="Change Cover Banner">
                        <i class="feather-camera" style="font-size:15px;"></i>
                    </button>
                </div>
                <div class="rbt-tutor-information-left" style="margin-top: -60px; position: relative; z-index: 1;">
                    <div class="thumbnail rbt-avatars size-lg" style="margin-left: 2rem; position: relative; display: inline-block;">
                        {% if app.user.logo %}
                            <img src="{{ asset('uploads/logos/' ~ app.user.logo) }}" alt="Professional" style="border-radius: 50%; width: 120px; height: 120px; object-fit: cover; border: 4px solid white; box-shadow: 0 4px 16px rgba(13,33,81,0.15);">
                        {% else %}
                            <img src="{{ asset('assets/drilleedo/images/team/avatar.jpg') }}" alt="Professional" style="border-radius: 50%; width: 120px; height: 120px; object-fit: cover; border: 4px solid white; box-shadow: 0 4px 16px rgba(13,33,81,0.15);">
                        {% endif %}
                        {# Photo edit button #}
                        <button class="pro-btn-icon" data-bs-toggle="modal" data-bs-target="#editLogoModal"
                                style="position: absolute; bottom: 4px; right: 4px; background: #00509e; border: 2px solid white; width: 32px; height: 32px; border-radius: 50%; color: white; display: flex; align-items: center; justify-content: center; cursor: pointer; box-shadow: 0 2px 8px rgba(0,0,0,0.15);"
                                title="Change Profile Photo">
                            <i class="feather-camera" style="font-size:13px;"></i>
                        </button>
                    </div>
                </div>
                <div class="mt-3 px-3">
                    <h5 class="mb-1" style="color: #0d2151; font-weight: 800;">{{ app.user.displayName }}</h5>
                    <p style="color: #64748b; font-size: 14px; margin: 0;">{{ app.user.email }}</p>
                </div>
            </div>

            <!-- Profile Information -->
            <div class="row g-4 mt-2">
                <div class="col-md-6">
                    <div class="d-flex align-items-start gap-3">
                        <div style="width: 44px; height: 44px; border-radius: 10px; background: #eff6ff; color: #00509e; display: flex; align-items: center; justify-content: center; font-size: 20px;">
                            <i class="feather-briefcase"></i>
                        </div>
                        <div class="flex-grow-1">
                            <p style="font-size: 12px; font-weight: 700; color: #94a3b8; text-transform: uppercase; margin-bottom: 2px;">Professional Name</p>
                            <p style="font-weight: 700; color: #0d2151; margin: 0;">{{ app.user.displayName }}</p>
                        </div>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="d-flex align-items-start gap-3">
                        <div style="width: 44px; height: 44px; border-radius: 10px; background: #f8fafc; color: #0d2151; display: flex; align-items: center; justify-content: center; font-size: 20px;">
                            <i class="feather-mail"></i>
                        </div>
                        <div class="flex-grow-1">
                            <p style="font-size: 12px; font-weight: 700; color: #94a3b8; text-transform: uppercase; margin-bottom: 2px;">Email</p>
                            <p style="font-weight: 700; color: #0d2151; margin: 0;">{{ app.user.email }}</p>
                        </div>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="d-flex align-items-start gap-3">
                        <div style="width: 44px; height: 44px; border-radius: 10px; background: #fff7ed; color: #e8501a; display: flex; align-items: center; justify-content: center; font-size: 20px;">
                            <i class="feather-map-pin"></i>
                        </div>
                        <div class="flex-grow-1">
                            <p style="font-size: 12px; font-weight: 700; color: #94a3b8; text-transform: uppercase; margin-bottom: 2px;">City</p>
                            <p style="font-weight: 700; color: #0d2151; margin: 0;">{{ app.user.city ?: 'Not set' }}</p>
                        </div>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="d-flex align-items-start gap-3">
                        <div style="width: 44px; height: 44px; border-radius: 10px; background: #eff6ff; color: #00509e; display: flex; align-items: center; justify-content: center; font-size: 20px;">
                            <i class="feather-award"></i>
                        </div>
                        <div class="flex-grow-1">
                            <p style="font-size: 12px; font-weight: 700; color: #94a3b8; text-transform: uppercase; margin-bottom: 2px;">Years of Experience</p>
                            <p style="font-weight: 700; color: #0d2151; margin: 0;">{{ app.user.expYears ?: 'Not set' }}</p>
                        </div>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="d-flex align-items-start gap-3">
                        <div style="width: 44px; height: 44px; border-radius: 10px; background: #f8fafc; color: #0d2151; display: flex; align-items: center; justify-content: center; font-size: 20px;">
                            <i class="feather-tag"></i>
                        </div>
                        <div class="flex-grow-1">
                            <p style="font-size: 12px; font-weight: 700; color: #94a3b8; text-transform: uppercase; margin-bottom: 2px;">Category</p>
                            <p style="font-weight: 700; color: #0d2151; margin: 0;">{{ app.user.category ? app.user.category.name : 'Not set' }}</p>
                        </div>
                    </div>
                </div>

                <div class="col-md-12 mt-4 pt-3 border-top">
                    <div class="d-flex align-items-start gap-3">
                        <div style="width: 44px; height: 44px; border-radius: 10px; background: #eff6ff; color: #00509e; display: flex; align-items: center; justify-content: center; font-size: 20px;">
                            <i class="feather-file-text"></i>
                        </div>
                        <div class="flex-grow-1">
                            <p style="font-size: 14px; font-weight: 800; color: #0d2151; margin-bottom: 6px;">About Me</p>
                            <p class="mb-0 text-break" style="color: #475569; line-height: 1.6;">{{ app.user.about ?: 'Not set' }}</p>
                        </div>
                    </div>
                </div>

                <div class="col-md-6 mt-4">
                    <div class="d-flex align-items-start gap-3">
                        <div style="width: 44px; height: 44px; border-radius: 10px; background: #f0fdf4; color: #16a34a; display: flex; align-items: center; justify-content: center; font-size: 20px;">
                            <i class="feather-check-circle"></i>
                        </div>
                        <div class="flex-grow-1">
                            <p style="font-size: 14px; font-weight: 800; color: #0d2151; margin-bottom: 6px;">Why Choose Me</p>
                            {% if app.user.whyChooseUs %}
                                {% if app.user.whyChooseUs is iterable %}
                                    <ul class="mb-0 ps-3" style="color: #475569; line-height: 1.6;">
                                        {% for item in app.user.whyChooseUs %}
                                            <li>{{ item }}</li>
                                        {% endfor %}
                                    </ul>
                                {% else %}
                                    <p class="mb-0" style="color: #475569; line-height: 1.6;">{{ app.user.whyChooseUs }}</p>
                                {% endif %}
                            {% else %}
                                <p class="mb-0" style="color: #94a3b8;">Not set</p>
                            {% endif %}
                        </div>
                    </div>
                </div>

                <div class="col-md-6 mt-4">
                    <div class="d-flex align-items-start gap-3">
                        <div style="width: 44px; height: 44px; border-radius: 10px; background: #fff7ed; color: #e8501a; display: flex; align-items: center; justify-content: center; font-size: 20px;">
                            <i class="feather-list"></i>
                        </div>
                        <div class="flex-grow-1">
                            <p style="font-size: 14px; font-weight: 800; color: #0d2151; margin-bottom: 6px;">Services</p>
                            {% if app.user.services %}
                                {% if app.user.services is iterable %}
                                    <ul class="mb-0 ps-3" style="color: #475569; line-height: 1.6;">
                                        {% for item in app.user.services %}
                                            <li>{{ item }}</li>
                                        {% endfor %}
                                    </ul>
                                {% else %}
                                    <p class="mb-0" style="color: #475569; line-height: 1.6;">{{ app.user.services }}</p>
                                {% endif %}
                            {% else %}
                                <p class="mb-0" style="color: #94a3b8;">Not set</p>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block script %}
    <script>
        document.getElementById('{{ form.logoFile.vars.id }}')?.addEventListener('change', function(event) {
            const [file] = event.target.files;
            if (file) {
                 const reader = new FileReader();
                 reader.onload = function(e) {
                     const img = document.getElementById('logo-preview');
                     if(img) img.src = e.target.result;
                 };
                 reader.readAsDataURL(file);
            }
        });

        document.getElementById('{{ form.bannerFile.vars.id }}')?.addEventListener('change', function(event) {
            const [file] = event.target.files;
            if (file) {
                 const reader = new FileReader();
                 reader.onload = function(e) {
                     const bannerDiv = document.getElementById('banner-preview-box');
                     if(bannerDiv) bannerDiv.style.backgroundImage = `url('${e.target.result}')`;
                 };
                 reader.readAsDataURL(file);
            }
        });
        // Reopen modal if form has errors
        {% if not form.vars.valid %}
            const editModal = new bootstrap.Modal(document.getElementById('editProfileModal'));
            editModal.show();
        {% endif %}
    </script>
{% endblock %}

{% block modals %}
    {{ parent() }}
    <!-- Edit Profile Modal -->
    <div class="modal fade" id="editProfileModal" tabindex="-1" aria-labelledby="editProfileModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-xl modal-dialog-centered">
            <div class="modal-content drl-modal-content">
                <div class="modal-header drl-modal-header">
                    <h5 class="modal-title drl-modal-title">
                        <i class="feather-user me-2"></i>Update Profile Information
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body drl-modal-body" style="max-height: 75vh; overflow-y: auto;">
                    {{ form_start(form, {'attr': {'class': 'row g-4', 'id': 'profileUpdateForm'}}) }}
                   
                        <div class="col-lg-6">
                            <div>
                                <label class="drl-label" for="{{ form.company_name.vars.id }}">Professional Name</label>
                                {{ form_widget(form.company_name, {'attr': {'class': 'drl-input', 'placeholder': 'John Doe Construction'}}) }}
                                {{ form_errors(form.company_name) }}
                            </div>
                        </div>

                        <div class="col-lg-6">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="drl-label" for="{{ form.parentCategory.vars.id }}">Industry/Sector</label>
                                    {{ form_widget(form.parentCategory, {'attr': {'class': 'drl-input form-select parent-category-select selectpicker', 'data-live-search': 'true'}}) }}
                                    {{ form_errors(form.parentCategory) }}
                                </div>
                                <div class="col-md-6">
                                    <label class="drl-label" for="{{ form.category.vars.id }}">Specific Service</label>
                                    {{ form_widget(form.category, {
                                        'attr': {
                                            'class': 'drl-input form-select child-category-select selectpicker',
                                            'data-selected-id': app.user.category ? app.user.category.id : '',
                                            'data-live-search': 'true'
                                        }
                                    }) }}
                                    {{ form_errors(form.category) }}
                                </div>
                            </div>
                        </div>

                        <div class="col-lg-6">
                            <div>
                                <label class="drl-label" for="{{ form.email.vars.id }}">Email</label>
                                {{ form_widget(form.email, {'attr': {'class': 'drl-input', 'placeholder': 'contact@example.com'}}) }}
                                {{ form_errors(form.email) }}
                            </div>
                        </div>

                        <div class="col-lg-6">
                            <div>
                                <label class="drl-label" for="{{ form.city.vars.id }}">City</label>
                                {{ form_widget(form.city, {'attr': {'class': 'drl-input', 'placeholder': 'Montreal'}}) }}
                                {{ form_errors(form.city) }}
                            </div>
                        </div>

                        <div class="col-lg-6">
                            <div>
                                <label class="drl-label" for="{{ form.exp_years.vars.id }}">Years of Experience</label>
                                {{ form_widget(form.exp_years, {'attr': {'class': 'drl-input', 'placeholder': '5'}}) }}
                                {{ form_errors(form.exp_years) }}
                            </div>
                        </div>

                        <div class="col-12 mt-4 pt-3 border-top">
                            <div>
                                <label class="drl-label">About Me</label>
                                {{ form_widget(form.about, {'attr': {'class': 'drl-input', 'rows': '4', 'placeholder': 'Tell clients about your business...'}}) }}
                                {{ form_errors(form.about) }}
                            </div>
                        </div>

                        <div class="col-lg-6">
                            <div>
                                <label class="drl-label">Why Choose Me <span class="small opacity-50 fw-normal">(One item per line)</span></label>
                                {{ form_widget(form.whyChooseUs, {'attr': {'class': 'drl-input', 'rows': '5'}}) }}
                                {{ form_errors(form.whyChooseUs) }}
                            </div>
                        </div>

                        <div class="col-lg-6">
                            <div>
                                <label class="drl-label">Services <span class="small opacity-50 fw-normal">(One item per line)</span></label>
                                {{ form_widget(form.services, {'attr': {'class': 'drl-input', 'rows': '5'}}) }}
                                {{ form_errors(form.services) }}
                            </div>
                        </div>
                    {{ form_end(form) }}
                </div>
                <div class="modal-footer drl-modal-footer">
                    <button type="button" class="btn btn-link text-muted text-decoration-none fw-bold" data-bs-dismiss="modal">
                        Cancel
                    </button>
                    <button type="submit" form="profileUpdateForm" class="drl-btn-orange px-5">
                        <i class="feather-save me-2"></i> Save Changes
                    </button>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
