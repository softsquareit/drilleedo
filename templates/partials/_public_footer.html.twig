{# Footer styles -> drilleedo.css (section: Footer) #}

<footer class="drld-footer" style="background-color: #04487e;">
    <div class="container" style="margin:auto ;">

        {# ── CTA Section ─────────────────────────────── #}
        <div class="f-cta">
            <div class="f-cta-row">
                <img
                    src="{{ asset('uploads/images/footer-text.png') }}"
                    alt="We connect Drilleedos to people who need help."
                    class="f-cta-img"
                >
                <a href="{{ path('professionals_list') }}" class="f-cta-btn">Get Free Quotes</a>
            </div>
        </div>

        {# ── Divider ─────────────────────────────────── #}
        <hr class="f-divider">

        {# ── 4 Columns ───────────────────────────────── #}
        <div class="f-links">
            <div class="f-grid">

                {# Col 1: Description #}
                <div>
                    <p class="f-desc">
                        Drilleedo brings together homeowners and trusted professionals across Canada.
                        Looking to collaborate or join the team? Reach out anytime!
                    </p>
                </div>

                {# Col 2: Useful Links #}
                <div>
                    <h5 class="f-col-title">Useful Links</h5>
                    <nav aria-label="Useful Links navigation">
                        <ul class="f-col-list">
                            <li><a href="{{ path('professionals_list') }}">Services</a></li>
                            <li><a href="{{ path('professionals_list') }}">Find Professionals</a></li>
                            <li><a href="{{ path('app_home') }}#how-it-works">How It Works</a></li>
                            <li><a href="{{ path('packages') }}">Pricing</a></li>
                            <li><a href="{{ path('contact') }}">FAQ</a></li>
                        </ul>
                    </nav>
                </div>

                {# Col 3: Top Cities #}
                <div>
                    <h5 class="f-col-title">Top Cities</h5>
                    <nav aria-label="Top Cities navigation">
                        <ul class="f-col-list">
                            <li><a href="{{ path('city_seo_landing', {citySlug: 'toronto'}) }}">Toronto</a></li>
                            <li><a href="{{ path('city_seo_landing', {citySlug: 'montreal'}) }}">Montreal</a></li>
                            <li><a href="{{ path('city_seo_landing', {citySlug: 'vancouver'}) }}">Vancouver</a></li>
                            <li><a href="{{ path('city_seo_landing', {citySlug: 'calgary'}) }}">Calgary</a></li>
                            <li><a href="{{ path('city_seo_landing', {citySlug: 'ottawa'}) }}">Ottawa</a></li>
                        </ul>
                    </nav>
                </div>

                {# Col 3: Company #}
                <div>
                    <h5 class="f-col-title">Company</h5>
                    <nav aria-label="Company navigation">
                        <ul class="f-col-list">
                            <li><a href="{{ path('app_about') }}">About Us</a></li>
                            <li><a href="{{ path('app_register') }}">Join as a Pro</a></li>
                            <li><a href="{{ path('public_blog_list') }}">Blog</a></li>
                            <li><a href="{{ path('app_careers') }}">Careers</a></li>
                            <li><a href="{{ path('contact') }}">Support</a></li>
                        </ul>
                    </nav>
                </div>

                {# Col 4: Get in Touch #}
                <div>
                    <h5 class="f-col-title">Get in Touch</h5>
                    <nav aria-label="Contact information">
                        <ul class="f-col-list">
                            <li>Phone: <a href="tel:+14065550120">(406) 555-0120</a></li>
                            <li>Email: <a href="mailto:contact@drilleedo.com">contact@drilleedo.com</a></li>
                            <li>Location: Toronto, Canada</li>
                        </ul>
                    </nav>
                </div>

            </div>
        </div>

    </div>{# /container #}

    {# ── Large Drilleedo® wordmark ───────────────────── #}
    <div class="f-wordmark">
        <img
            src="{{ asset('uploads/images/Logo_bottom.png') }}"
            alt="Drilleedo"
            loading="lazy"
        >
    </div>

  
</footer>
