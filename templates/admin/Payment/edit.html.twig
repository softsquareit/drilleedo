{% extends 'base.html.twig' %}

{% block title %}Edit Type{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1>Edit Type</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Update</button>
    {{ form_end(form) }}
    <a href="{{ path('admin_type_index') }}" class="btn btn-secondary">Back</a>
</div>
{% endblock %}